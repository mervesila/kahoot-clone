services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: kahoot_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d kahoot_db"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: .
      dockerfile: src/WebAPI/TKI.WebAPI/Dockerfile
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/kahoot_db
      JWT_KEY: ${JWT_KEY:-change-me-to-a-long-random-secret-of-32+-chars}
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
